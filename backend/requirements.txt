Django==4.2.7
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.0
django-cors-headers==4.3.0
mysqlclient==2.2.0
python-decouple==3.8
Pillow==10.1.0
django-cors-headers
openai>=1.12.0          # API OpenAI pour l'analyse IA
pypdf>=4.0.0            # Extraction de texte depuis PDF
python-dotenv>=1.0.0    # Gestion des variables d'environnement

# Optional dependencies
pdfplumber>=0.10.0      # Extraction avancée de tableaux (optionnel)
pytesseract>=0.3.10     # OCR pour PDF scannés (optionnel)
pdf2image>=1.16.0       # Conversion PDF vers images pour OCR (optionnel)

